import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DeuxPage } from './deux';

@NgModule({
  declarations: [
    DeuxPage,
  ],
  imports: [
    IonicPageModule.forChild(DeuxPage),
  ],
})
export class DeuxPageModule {}
