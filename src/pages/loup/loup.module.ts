import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LoupPage } from './loup';

@NgModule({
  declarations: [
    LoupPage,
  ],
  imports: [
    IonicPageModule.forChild(LoupPage),
  ],
})
export class LoupPageModule {}
